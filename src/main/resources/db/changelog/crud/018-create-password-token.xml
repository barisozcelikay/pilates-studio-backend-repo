<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="018-create-password-token" author="baris">

        <createTable
                schemaName="pilates_studio"
                tableName="password_token">

            <column
                    name="id"
                    type="BIGINT"
                    autoIncrement="true">
                <constraints
                        primaryKey="true"
                        nullable="false"/>
            </column>

            <column
                    name="account_id"
                    type="BIGINT">
                <constraints
                        nullable="false"
                        foreignKeyName="fk_password_token_account"
                        referencedTableSchemaName="pilates_studio"
                        referencedTableName="account"
                        referencedColumnNames="id"/>
            </column>

            <column
                    name="token_hash"
                    type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column
                    name="token_type"
                    type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>

            <column
                    name="expires_at"
                    type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column
                    name="used_at"
                    type="TIMESTAMP WITH TIME ZONE"/>

            <column
                    name="created_at"
                    type="TIMESTAMP WITH TIME ZONE"
                    defaultValueDate="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column
                    name="updated_at"
                    type="TIMESTAMP WITH TIME ZONE"
                    defaultValueDate="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <addUniqueConstraint
                schemaName="pilates_studio"
                tableName="password_token"
                columnNames="token_hash"
                constraintName="uk_password_token_hash"/>

        <createIndex
                schemaName="pilates_studio"
                tableName="password_token"
                indexName="idx_password_token_account_id">

            <column name="account_id"/>

        </createIndex>

    </changeSet>

</databaseChangeLog>
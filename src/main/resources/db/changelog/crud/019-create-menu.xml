<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="019-create-menu" author="baris">

        <createTable
                schemaName="pilates_studio"
                tableName="menu">

            <column
                    name="id"
                    type="BIGINT"
                    autoIncrement="true">
                <constraints
                        primaryKey="true"
                        primaryKeyName="pk_menu"
                        nullable="false"/>
            </column>

            <column
                    name="created_at"
                    type="TIMESTAMP WITHOUT TIME ZONE"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column
                    name="updated_at"
                    type="TIMESTAMP WITHOUT TIME ZONE"
                    defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column
                    name="code"
                    type="VARCHAR(50)">
                <constraints
                        nullable="false"
                        unique="true"
                        uniqueConstraintName="uk_menu_code"/>
            </column>

            <column
                    name="name"
                    type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column
                    name="icon"
                    type="VARCHAR(100)">
                <constraints nullable="true"/>
            </column>

            <column
                    name="route"
                    type="VARCHAR(255)">
                <constraints nullable="true"/>
            </column>

            <column
                    name="parent_id"
                    type="BIGINT">
                <constraints nullable="true"/>
            </column>

            <column
                    name="sort_order"
                    type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column
                    name="active"
                    type="BOOLEAN"
                    defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <addForeignKeyConstraint
                baseTableSchemaName="pilates_studio"
                baseTableName="menu"
                baseColumnNames="parent_id"
                referencedTableSchemaName="pilates_studio"
                referencedTableName="menu"
                referencedColumnNames="id"
                constraintName="fk_menu_parent"/>

    </changeSet>

</databaseChangeLog>